<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:label="Digitor"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitorAndroid">

        <!--
             Android 12+ hides non-NDK vendor libraries unless the app requests them explicitly.
             Paddle Lite's Mali OpenCL runtime may dlopen libOpenCL.so and its libGLES_mali.so
             driver dependency. Keep both optional so installation remains compatible on non-Mali
             devices while allowing the Symphony/UniSOC Mali path to enter the vendor namespace.
             A real 512x512 Paddle Lite warm-up still has to succeed before Cutout reports GPU.
        -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libGLES_mali.so" android:required="false" />
        <uses-native-library android:name="libvndksupport.so" android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
