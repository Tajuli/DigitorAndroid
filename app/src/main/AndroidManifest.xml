<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- WhisperKit's published Android runtime declares API 26. Digitor still installs on API 24/25;
         Auto CC is runtime-gated to Android 8.0+ and never loads its native library below that. -->
    <uses-sdk tools:overrideLibrary="com.argmaxinc.whisperkit" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:label="Digitor"
        android:supportsRtl="true"
        android:theme="@style/Theme.DigitorAndroid">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
