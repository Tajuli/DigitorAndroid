# V24 transition rendering

V24 keeps V22 as the compatibility render family and adds preset-specific geometry/shader layers.
Unknown or missing preset ids must always fall back to the corresponding V22 style.
